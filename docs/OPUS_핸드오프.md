# 과거 Opus 핸드오프 프롬프트 (2026-07-16)

> **현재 작업 지시가 아니다.** 신규 작업은 `docs/제품_기획.md`를 먼저 읽고 그 제품 계약을 따른다.

> 아래 프롬프트를 Opus 세션의 첫 메시지로 붙여넣는다. (모델: Opus 4.8, 브랜치: 본인 브랜치에서)

---

쉼표 정류장(2026 춘천시 데이터 활용 해커톤 본선작) 본선 준비 개발을 맡긴다. 계획서의 Phase A를 태스크 단위로 실행해줘.

## 읽기 순서 (작업 시작 전 필수)

1. `CLAUDE.md` + `AGENTS.md` — 팀 규칙(브랜치·커밋·PR)
2. `docs/superpowers/plans/2026-07-16-본선.md` — **실행 기준 문서. 최상단 🛑 즉시 중단 조건 3가지를 가장 먼저 읽고 전 작업에서 준수할 것**
3. `docs/specs/2026-07-16-본선-design.md` — 설계(산식·판정 기준·문구 규칙)
4. `docs/사용자_흐름.md` — B2C/B2G 사용자 흐름 상세
5. `docs/본선_전략.md` — 왜 이렇게 설계됐는지(심사 배점 매핑)
6. `docs/데이터_현황.md` — 데이터 실측과 정직성 규칙
7. (배경) `docs/superpowers/plans/codex_반박의견.md` — 이 설계가 통과한 비판 검토. 구현이 이 문서의 지적을 다시 범하면 안 된다.

## 실행 방식

- superpowers:subagent-driven-development로 계획서 Phase A를 **A1부터 순서대로** 실행. 각 태스크는 실패 테스트 먼저(TDD), 통과 후 명시된 경로만 `git add`하여 커밋.
- 커밋 메시지 끝: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. main 직접 push 금지(AGENTS.md).
- 프로젝트 에이전트 7종(`.claude/agents/`)이 v3.1 기준으로 갱신되어 있다 — 태스크 성격에 맞는 에이전트에 위임하라(데이터-pipeline, 프론트-frontend, 백엔드-infra, 보안-security, 디자인-a11y, qa-검증, 총괄-techlead).

## 🛑 즉시 중단 조건 (계획서와 동일 — 위반 시 작업을 멈추고 보고)

1. unknown이 설치 검토 후보(InstallRow)에 들어오는 순간
2. 검수되지 않은 로드뷰 AI 초안이 no를 생성하는 순간
3. 데이터 품질 검증(A1)·사용자 테스트(A10) 완료 전에 P2 기능(카운터·AI 브리핑)에 착수하는 순간

테스트를 수정해 통과시키는 우회 금지. 테스트가 거슬리면 테스트가 아니라 접근을 고쳐라.

## 사람 개입 게이트 (도달하면 자동화하지 말고 준비물만 만들고 보고 후 대기)

- **A5**: 로드뷰 캡처·검수는 팀원이 한다. 너는 대상 목록·프롬프트·변환기까지 만들고, 캡처 폴더가 채워지기 전엔 픽스처로 테스트만 통과시켜라. 미검수 초안을 `data/`에 넣지 마라.
- **A6**: POI CSV 3종은 팀원이 다운로드한다. 파일 부재 시 skip 경로가 정상 동작해야 한다.
- **A10**: 고령 사용자 테스트는 사람 태스크 — 체크리스트·기록 양식만 준비.
- **A13**: 배포 URL은 팀 결정 — 결정 전엔 포스터 QR을 최종 생성하지 마라.

## 불변 사항

- `app/src/types/stop.ts`와 stops.json 스키마는 계약이다 — 변경 금지(필요해 보이면 구현하지 말고 보고).
- 기존 v1/v2 기능(지도·시설필터·즐겨찾기·공유·인쇄·/go·오프라인·기존 대시보드 필터 탭)은 전부 보존. 현재 테스트 103개(vitest)·39개(pytest) 전부 통과 상태에서 시작한다 — 깨뜨리면 그 태스크 안에서 복구.
- 표현 금지어: "현장 확인", "수혜", "일평균", "그늘 4곳뿐", "민감도 분석"(→"정책 시나리오 비교"), 조건 없는 "오프라인 동작", B2C 화면의 점수 숫자, 의자 미확인 카드의 "앉아서".
- 공개 배포는 무키 빌드(`VITE_TAGO_KEY` 비움). 클라이언트 번들에 LLM 등 과금 키 절대 금지.

## 보고 형식

태스크 하나 끝날 때마다: 태스크 번호 / 통과한 테스트 수(실제 실행 출력) / 커밋 해시 / 다음 태스크. Phase A 완료 시: 전체 테스트 결과 + 사람 개입 게이트 대기 목록 + 남은 리스크.

기능 추가 제안은 하지 마라. 대상 가능성은 ①조사 CSV 투입 전후 수치 라이브 데모 ②데이터 검증 리포트 ③사용자 테스트의 안정적 완성에서 나온다.

A1(데이터 품질 검증 리포트)부터 시작해줘.
